version: '3.8'

services:
  # Greenmask container that processes dump.sql file
  greenmask:
    build:
      context: .
      dockerfile: Dockerfile.greenmask
    volumes:
      - ./dump:/input
      - ./output:/output
      - ./transformers:/app/transformers
      - ./config:/configs
    environment:
      - OFOX_API_KEY=${OFOX_API_KEY}
    working_dir: /app
    command: >
      greenmask sanitise \
      --source '/input/dump.sql' \
      --target '/output/sanitized.sql.gz'
    depends_on: []
